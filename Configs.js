export default class CanvasConfigs {
  static width = 500;
  static height = 600;
  static middleWidth = this.width / 2;
  static middleHeight = this.height / 2;
  static commonStyle = {
    shadow: "#fff",
    blur: 20,
    lineJoin: "bevel",
    lineWidth: 3,
  };
}
